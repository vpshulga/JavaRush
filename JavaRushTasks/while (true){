            String str = reader.readLine();
            writer.write(str + "\n");
            writer.flush();
            if (str.equals("exit")){
                break;
            }
        }
        reader.close();
